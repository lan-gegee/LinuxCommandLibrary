# TAGLINE

解压由 lrzip 压缩的文件

# TLDR

该命令是 lrzip --decompress 的**别名**

```tldr lrzip```

# SYNOPSIS

**lrunzip** [_options_] [_file_]

# DESCRIPTION

**lrunzip** 解压由 lrzip 压缩的文件，等价于运行 `lrzip --decompress`。

# INSTALL

```apt: sudo apt install lrzip```

```pacman: sudo pacman -S lrzip```

```apk: sudo apk add lrzip```

```zypper: sudo zypper install lrzip```

```brew: brew install lrzip```

```nix: nix profile install nixpkgs#lrzip```

<!-- packages: 2026-07-22 -->

# SEE ALSO

[lrzip](/man/lrzip)(1), [lrztar](/man/lrztar)(1)

# TAGLINE

词法分析器生成器，在大多数现代系统上传统上是 **flex** 的别名

# TLDR

此命令是 flex 的**别名**

```tldr flex```

# SYNOPSIS

**lex** [_options_] [_file_]

# DESCRIPTION

**lex** 是一个词法分析器生成器，在大多数现代系统上传统上是 **flex**（fast lexical analyzer generator，快速词法分析器生成器）的别名。它基于正则表达式模式生成用于对输入进行分词的 C 代码。

# INSTALL

```apt: sudo apt install flex```

```dnf: sudo dnf install flex```

```pacman: sudo pacman -S flex```

```apk: sudo apk add flex```

```zypper: sudo zypper install flex```

```brew: brew install flex```

```nix: nix profile install nixpkgs#flex```

<!-- packages: 2026-07-22 -->

# SEE ALSO

[flex](/man/flex)(1), [yacc](/man/yacc)(1), [bison](/man/bison)(1)
